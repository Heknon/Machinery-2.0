package me.oriharel.machinery.structure.schematic

enum class SchematicOption {
    OVERWRITE_BLOCKS,
    BUILD_PREVIEW,
    SUMMON_DEFAULT_PARTICLES,
    PLAY_DEFAULT_SOUND,
    PREVENT_BREAK_WHILE_BUILD
}